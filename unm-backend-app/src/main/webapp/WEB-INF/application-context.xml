<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:jpa="http://www.springframework.org/schema/data/jpa"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/data/jpa
    http://www.springframework.org/schema/data/jpa/spring-jpa.xsd
    http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.2.xsd">

	<!-- Database -->
	<bean id="datasource" class="org.springframework.jndi.JndiObjectFactoryBean">
		<property name="jndiName" value="java:comp/env/jdbc/univmobile"/>
	</bean>
	
	<!-- Entity Manager -->
	<bean id="entityManagerFactory"
		class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
		<property name="dataSource" ref="datasource" />
		<property name="persistenceUnitName" value="domain-persistence" />
	</bean>

	<!-- Transaction Manager -->
	<bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="entityManagerFactory" />
	</bean>

	<!-- Jpa Repositories -->
	<jpa:repositories base-package="fr.univmobile.backend.domain"></jpa:repositories>

	<!-- Test Auditor -->
	<bean id="sessionAuditorAware" class="fr.univmobile.backend.SessionAuditorAware">
	</bean>

	<jpa:auditing auditor-aware-ref="sessionAuditorAware" />

	<security:http use-expressions="true" auto-config="true">
        <!--remove createdBy and updatedBy patterns when https://jira.spring.io/browse/DATAREST-87 is fixed-->
        <security:intercept-url pattern="/**/createdBy" access="hasRole('superadmin')"/>
        <security:intercept-url pattern="/**/updatedBy" access="hasRole('superadmin')"/>
        <security:intercept-url pattern="/**" access="permitAll" />
		<security:form-login login-page="/"/>
		<security:custom-filter ref="compositePreAuthenticationFilter" position="PRE_AUTH_FILTER" />
	</security:http>

	<bean id="unimobileAuthenticationProvider" class="fr.univmobile.backend.security.UnimobileAuthenticationProvider">
		<property name="userRepository" ref="userRepository"/>
	</bean>

	<security:authentication-manager alias="authenticationManager">
		<security:authentication-provider ref="unimobileAuthenticationProvider"/>
	</security:authentication-manager>

	<security:global-method-security pre-post-annotations="enabled"/>
	
	<bean id="unimobilePreAuthenticationFilter" class="fr.univmobile.backend.security.UnimobilePreAuthenticationFilter">
		<property name="authenticationManager" ref="authenticationManager" />
	</bean>

	<bean id="unimobileTokenPreAuthenticationFilter" class="fr.univmobile.backend.security.UnimobileTokenPreAuthenticationFilter">
		<property name="authenticationManager" ref="authenticationManager" />
		<!-- <property name="tokenRepository" ref="tokenRepository"/> -->
		<property name="authenticatedSessionRepository" ref="authenticatedSessionRepository"/>
		<property name="sessionAuditorAware" ref="sessionAuditorAware"/>
	</bean>

	<bean id="compositePreAuthenticationFilter" class="org.springframework.web.filter.CompositeFilter">
		<property name="filters">
			<list>
				<ref bean="unimobileTokenPreAuthenticationFilter"/>
				<ref bean="unimobilePreAuthenticationFilter"/>
			</list>
		</property>
	</bean>
	<bean id="securityEvaluationContextExtension" class="org.springframework.security.data.repository.query.SecurityEvaluationContextExtension"/>
</beans>